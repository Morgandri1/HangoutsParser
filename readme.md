Parse Hangouts chat history into text files

### Usage 
python3 main.py <file_full_path> <start_timestamp>

Start timestamp is the first timestamp from ``messages.json`` which you'd like to parse from.